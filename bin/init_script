#!/usr/bin/env bash

# Set super(Windows) key to be alt+R. In settings this is mapped to rofi
ksuperkey -e 'Super_L=Alt_L|R'

# Start polybar
~/.config/polybar/launch.sh --forest


# Kill running instance of onedrive since it gets messed up 
killall onedrive

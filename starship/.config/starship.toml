"$schema" = 'https://starship.rs/config-schema.json'
add_newline = true

format = """
$username\
$hostname\
$localip\
$shlvl\
$directory\
$git_branch\
$git_commit\
$git_state\
$git_metrics\
$git_status\
$env_var\
$python\
$sudo\
$line_break\
$jobs\
$battery\
$time\
$status\
$os\
$container\
$shell\
$character"""

[character]
success_symbol = '[➜](bold purple)'

[package]
disabled = true

[python]
# only show virtual env
# format='[${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
format='[${pyenv_prefix}(\($virtualenv\) )]($style)'
